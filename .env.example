# Railway / cloud deployment
# Choose one persistence strategy:
# 1. Set DATABASE_URL to a managed database connection string, or
# 2. Set APP_DATA_DIR to a writable mounted volume path such as /data for SQLite persistence.

DATABASE_URL=
APP_DATA_DIR=
